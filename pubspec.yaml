name: dlna
description: A simple DLNA DMC library implemented by Dart. It is tiny and only
  the basic network video casting function is supported.
homepage: https://github.com/succlz123/DLNA-Dart
version: 0.0.5

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  connectivity_plus: ^2.1.0
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.9
  xml2json: ^5.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: null
